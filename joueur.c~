#include "joueur.h"

void action_player(ElementSDL2 *this){
  static float speed = 0;
  Player * data;
  float a;
  
  if(this){
    data=getDataElementSDL2(this);
    if(data){
      getAngleElementSDL2(this,&a);
      if(
    }
  }
}

void click_player(ElementSDL2 * this,SDL_Keycode c){
  Player * data;
  printf("%d\n",c);
  if(this){
    data=getDataElementSDL2(this);
    if(data && !data->up && c==32){
      data->up=1;
    }
  }
}
